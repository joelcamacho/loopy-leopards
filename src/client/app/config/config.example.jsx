// Firebase config stuff
const config = {
  apiKey: "< fcm from firebase console api key>",
  authDomain: "< project id from firebase console>.firebaseapp.com",
  databaseURL: "<firebase database url>",
  messagingSenderId: "< sender id from fcm>"
};

export default config;